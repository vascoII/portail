meta {
  name: PatchOperatorAction
  type: http
  seq: 5
}

patch {
  url: {{localhost_backend}}/api/operator/79252
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bearToken}}
}

body:json {
  {
      "password": "password"
  }
}

settings {
  encodeUrl: true
}
