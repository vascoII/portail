meta {
  name: GetParcAction
  type: http
  seq: 9
}

get {
  url: {{localhost_backend}}/api/parc
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearToken}}
}

settings {
  encodeUrl: true
}
